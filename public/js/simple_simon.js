"use strict";
(function(){
	var colors = ["green", "red", "yellow", "blue"];
	var playing = false;
	var colorQueue = [];

	function playGame(){
		var random;

		while(playing){
			random = Math.floor(Math.random() * colors.length);
			
		}
	}
})();